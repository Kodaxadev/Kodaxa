---
interface Props {
  image: string;
  label: string;
  title: string;
}
const { image, label, title } = Astro.props;
---
<figure class="project-visual">
  <img src={image} alt={`${title} — ${label}`} loading="lazy" />
  <figcaption>{label}</figcaption>
</figure>
